services:
  - type: web
    name: vibecraft-auth-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.api.api:app --host 0.0.0.0 --port 8000
    envVars:
      - key: DEBUG
        value: false

